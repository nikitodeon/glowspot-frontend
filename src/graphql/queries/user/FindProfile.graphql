query FindProfile {
	findProfile {
		id
		username
		displayName
		email
		avatar
		bio
		isAdmin
		isVerified
		isTotpEnabled
		notificationSettings {
			siteNotifications
			telegramNotifications
		}
	}
}
